# rebuild/ — HK Vastgoed

Output of **Stage 5 (Rebuild)**. The new **Django + React** implementation.

Start from:
- `templates/django-react-starter` — base project skeleton
- `templates/tailwind-components` — reusable UI
- `templates/seo` — SEO boilerplate

Port content from `../scraped/`, assets from `../assets/`, and apply the
improvements from `../analysis/` and `../docs/`. Preserve important URLs with
redirects to protect existing SEO.
