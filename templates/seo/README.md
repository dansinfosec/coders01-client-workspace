# seo

Reusable **SEO boilerplate** for client rebuilds.

Intended contents (to be added):
- `meta.html` / component — title, description, canonical, viewport
- `open-graph.html` — OG + Twitter card tags
- `schema/` — JSON-LD templates (Organization, LocalBusiness, RealEstateAgent,
  BreadcrumbList, etc.)
- `robots.txt` — template with sensible defaults
- `sitemap` — sitemap generation notes / template

> Placeholder for now. See `automation/sitemap-generator` for the sitemap tool.
