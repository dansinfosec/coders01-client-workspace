# Internal preview note — Gentleman's Touch

**These Google Maps photos are temporary preview assets and must be replaced with
owner-provided originals before the production website is delivered.**

- Google preview photos live in `public/images/google-preview/` and are
  **git-ignored** (not committed). Source: Google Maps place "Gentleman's touch",
  Vlaardingen. Attribution preserved in `src/data/gallery.ts`.
- The wordmark in the header/hero is a **temporary text-based wordmark**, not a
  final logo. The owner's real logo artwork (from Setmore) is a phone screenshot
  containing phone UI and conflicting names, so it is not used publicly.
- Do not deploy and do not commit while these temporary assets are in place.

See `../docs/` for the full source report and branding-mismatch details.
