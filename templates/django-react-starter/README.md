# django-react-starter

Base **Django + React** project skeleton, copied into `clients/<client>/rebuild/`
at the start of Stage 5.

Intended shape (to be scaffolded):
- `backend/` — Django project (API, models, admin, settings)
- `frontend/` — React app (Vite or similar) styled with Tailwind
- Integration wiring (CORS/proxy), env config, and a dev run script

> Placeholder for now — the actual starter code will be added when the first
> rebuild begins.
