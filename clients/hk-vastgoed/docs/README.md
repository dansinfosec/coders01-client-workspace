# docs/ — HK Vastgoed

Output of **Stage 4 (Document)**. The rebuild-ready specification, produced from
the analysis using `automation/ai-content-generator` + `prompts/`.

Suggested documents:
- `sitemap.md` — new site structure + URL map (with redirects from old URLs)
- `content-model.md` — page types, sections, fields, reusable components
- `rebuild-spec.md` — what to build, what to improve, acceptance criteria
