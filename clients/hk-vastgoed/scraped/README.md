# scraped/ — HK Vastgoed

Raw output of **Stage 1 (Crawl)**. Produced by `automation/crawler`.

Suggested contents:
- `pages/` — raw HTML per URL
- `text/` — extracted plain text per URL
- `urls.json` — inventory of discovered URLs + status codes

Do not hand-edit these files; they are regenerated by re-crawling.
