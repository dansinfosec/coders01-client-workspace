# Internal preview note — Two in one barberstudio

**These Google Maps photos are temporary preview assets and should be replaced
with owner-provided originals before the production website is delivered.**

- Optimized Google preview photos live in `public/images/google-preview/` (the
  7 `.webp` files are tracked so Vercel can display them; raw downloads and the
  contact sheet stay git-ignored). Source: the VERIFIED Google Maps listing
  "Two in one barberstudio", Visseringlaan 19, Rijswijk. Attribution preserved in
  `src/data/gallery.ts`.
- The wordmark in the header/hero is a **temporary text-based wordmark**, not a
  final logo (the owner's logo artwork is only available as a phone screenshot).
- Do not deploy, commit or push while these temporary assets are in place.

See `../docs/SOURCE-REPORT.md` for the full source report.
