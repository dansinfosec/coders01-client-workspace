# Secrets — never commit the real key.
.env
.env.*
!.env.example

# Generated run artifacts.
output/

# Local-only backups (may contain personal lead data) — never tracked.
backups/

# Local venv / caches.
.venv/
__pycache__/
*.py[cod]

# Generated Graphify code graph
graphify-out/
