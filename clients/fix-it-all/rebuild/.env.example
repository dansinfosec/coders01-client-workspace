# Fix-it All — environment variables (names + purpose only; never commit real values).

# Optional: when set, the appointment mock-service will POST to this endpoint instead of
# simulating. Empty = frontend-only mock. Real backend lands in a later phase.
VITE_APPOINTMENT_ENDPOINT=

# Optional: when set, the vehicle-sale mock-service will POST leads/photos here instead of
# simulating. Empty = frontend-only mock (Django/DRF backend lands in phase 8).
VITE_VEHICLE_SALE_ENDPOINT=
