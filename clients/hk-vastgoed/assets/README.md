# assets/ — HK Vastgoed

Output of **Stage 2 (Assets)**. Produced by `automation/image-downloader`.

Suggested contents:
- `images/` — downloaded images
- `fonts/` — downloaded fonts
- `media/` — video/audio/other media
- `manifest.json` — original URL → local path mapping (for remapping in rebuild)

Assets belong to the client / original owner — use only to rebuild this site.
