# Automotive starter — environment variables (names + purpose only; never commit real values).

# Optioneel: als dit is ingesteld, POST de afspraak-mockservice naar dit endpoint i.p.v.
# te simuleren. Leeg = frontend-only mock (nog geen backend).
VITE_APPOINTMENT_ENDPOINT=

# Optioneel: als dit is ingesteld, POST de auto-verkoop-mockservice de leads/foto's hierheen
# i.p.v. te simuleren. Leeg = frontend-only mock (nog geen backend).
VITE_VEHICLE_SALE_ENDPOINT=
